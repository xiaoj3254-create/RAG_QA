# =====================================================================
# RAG QA System 环境变量模板
# 复制本文件为 .env 后填入真实密钥：
#   Linux/macOS: cp .env.example .env
#   Windows:     copy .env.example .env
# 不填密钥则以「降级演示模式」运行（SimpleEmbeddings + 无 LLM 回答），
# 全流程仍可跑通，但检索质量与回答质量均不可用于生产。
# =====================================================================

# ---------------------------------------------------------------------
# 路径配置（一般保持默认即可）
# ---------------------------------------------------------------------
CHROMA_DB_PATH=./db/chroma_db
SQLITE_DB_PATH=./db/rag.db
LOG_FILE=./logs/rag.log
MAX_UPLOAD_MB=10

# ---------------------------------------------------------------------
# RAG 超参数
# ---------------------------------------------------------------------
CHUNK_SIZE=500
CHUNK_OVERLAP=100
TOP_K=3
MAX_RETRY=2
TEMPERATURE=0.1
MAX_TOKENS=1000

# ---------------------------------------------------------------------
# 模型名
# ---------------------------------------------------------------------
DEEPSEEK_MODEL=deepseek-ai/DeepSeek-V4-Flash
OPENAI_EMBEDDING_MODEL=BAAI/bge-m3
RERANKER_MODEL_NAME=BAAI/bge-reranker-v2-m3
MULTI_QUERY_NUM=3

# 本地重排模型相关：重排链路为「本地 CrossEncoder 优先 → 硅基流动 /rerank API → 原顺序」。
# 若本地已缓存某重排模型（如 BAAI/bge-reranker-base），把上面 RERANKER_MODEL_NAME 改成它，
# 并开启离线模式可彻底避免联网下载；未缓存时保持默认 v2-m3 并注释掉下面这行。
# HF_HUB_OFFLINE=1

# ---------------------------------------------------------------------
# API 密钥与接入地址
# LLM 与 Embedding 默认统一走硅基流动（SiliconFlow），两个 key 通常填同一个值
# ---------------------------------------------------------------------
OPENAI_API_BASE=https://api.siliconflow.cn/v1
DEEPSEEK_API_KEY=
OPENAI_API_KEY=

# ---------------------------------------------------------------------
# 功能开关
# ---------------------------------------------------------------------
ENABLE_MULTI_QUERY=1
ENABLE_RERANKER=1