# RAG 检索增强生成问答系统 —— 完整项目分析文档

> 本文档基于对项目全部源码的逐行阅读撰写，所有函数名、参数、默认值、分支逻辑均来自当前代码实际实现。

---

## 一、整体概览

### 1.1 项目架构与 RAG 类型

**项目形态**：前后端分离的 Web 版 RAG 问答系统。

```
Streamlit 前端 (frontend.py，纯 HTTP 客户端)
        │  REST / JSON（requests）
        ▼
FastAPI 后端 (api.py，/api/* 接口)
        │
        ├──► RAG 核心 (main.py)：LangGraph 状态图编排
        │        ├── DocumentProcessor：切片 + Embedding + Chroma
        │        ├── Retriever：单路 / Multi-Query 多路召回
        │        └── Generator：改写 / 生成 / 置信度 / 幻觉检测
        ├──► Chroma 向量库（db/chroma_db/，磁盘持久化，存向量+文本块）
        └──► SQLite（db/rag.db，只存会话/消息/文档元数据，不存向量）
```

**RAG 类型判定：属于 "Advanced RAG（高级 RAG）"，并带有 Self-RAG 风格的生成后自检-重试闭环**，而不是最基础的 Naive RAG。判定依据是代码中三个阶段都有增强：

| 阶段 | 本项目实现 | 代码位置 |
|---|---|---|
| 检索前（Pre-retrieval） | 多轮对话查询改写（`Generator.rewrite_query`）+ Multi-Query 子查询扩展（`multi_query_generate`） | main.py、reranker_helper.py |
| 检索后（Post-retrieval） | Cross-Encoder 重排（`rerank_documents`） | reranker_helper.py |
| 生成后（Post-generation） | LLM 幻觉检测 → LangGraph 条件边回退重生成（带 `max_retry` 防死循环 + 纠偏 feedback） | main.py `hallucination_check_node` |

**为什么不是严格意义上的 CRAG（Corrective RAG）**：CRAG 的核心是「检索质量评估器」对召回文档做 correct/ambiguous/incorrect 三态判定，incorrect 时转向 Web 搜索等外部知识。本项目没有检索质量评估器，也没有外部知识源补充；它的自检发生在**生成之后**（判断回答是否与上下文冲突），重试时上下文不变、只通过 feedback 提示词约束 LLM 重新作答。因此更准确的描述是：**Advanced RAG + 轻量级 Self-RAG 反思循环**。

### 1.2 技术栈

| 类别 | 选型 | 说明 |
|---|---|---|
| LLM 编排框架 | LangChain 0.3 系（langchain / core / community / text-splitters / openai） | Prompt、Document、Splitter、init_chat_model |
| 流程编排 | LangGraph ≥0.2 | 7 节点状态图 + 条件边 |
| 向量数据库 | Chroma（langchain-chroma + chromadb） | collection 名 `rag_docs`，磁盘持久化 |
| Embedding | OpenAI 兼容接口（默认硅基流动 SiliconFlow，模型 `BAAI/bge-m3`）；无密钥降级 `SimpleEmbeddings`（MD5 哈希伪向量，384 维） | main.py `get_embeddings()` |
| 生成 LLM | OpenAI 兼容协议（`init_chat_model(model_provider="openai")`，默认模型 `deepseek-ai/DeepSeek-V4-Flash`，base_url 指硅基流动） | main.py `deepseek_model()` |
| 重排器 | **三级降级**：本地 sentence-transformers CrossEncoder 优先（单例缓存、无网络依赖）→ 硅基流动 `/rerank` HTTP API 兜底（模型 `BAAI/bge-reranker-v2-m3`）→ 原顺序返回 | reranker_helper.py |
| 文档解析 | langchain_community：PyPDFLoader / TextLoader / JSONLoader / CSVLoader | file_loader.py |
| 后端 | FastAPI + uvicorn（自动生成 /docs） | api.py |
| 前端 | Streamlit（requests 调 REST，0 依赖 RAG 内部类） | frontend.py |
| 元数据存储 | SQLite 标准库（WAL 模式、外键、timeout=15s） | db/sqlite_db.py |
| 配置 | python-dotenv 读 .env，全部字段带默认值 | config.py |

### 1.3 目录与文件职责

```
RAG/
├── config.py              # 全局配置中心：路径、超参、模型名、密钥、开关（全部从 .env 读，带默认值）
├── main.py                # RAG 核心：SimpleEmbeddings / RAGConfig / RAGState /
│                          #   DocumentProcessor / Retriever / Generator / RAGChain(LangGraph)
├── api.py                 # FastAPI 服务层：上传/删除文档、问答、会话增删查、健康检查；RAGChain 全局单例
├── frontend.py            # Streamlit 前端：侧边栏（状态/上传+文档删除/参数/会话增删）+ 主区（对话/来源/调试面板）
├── smoke_api_test.py      # 冒烟测试：TestClient 串测全部接口场景
├── requirements.txt       # 依赖清单
├── Dockerfile             # 双进程镜像（uvicorn:8000 + streamlit:8501）
├── .env.example           # 环境变量模板
├── db/
│   ├── sqlite_db.py       # SQLite 三表元数据 CRUD（sessions / messages / uploaded_docs，含会话/文档删除）
│   └── chroma_db/         # Chroma 持久化目录（git 忽略）
└── utils/
    ├── file_loader.py     # 多格式文件解析 → List[Document]
    ├── reranker_helper.py # Multi-Query 生成 + 重排（本地 CrossEncoder 优先，硅基流动 /rerank API 兜底）
    └── logger.py          # 统一日志（控制台 + 可选文件，Windows UTF-8 兜底）
```

---

## 二、业务代码文件逐个解析

### 1. db/sqlite_db.py

**文件定位**：系统的元数据持久层。只存三类元数据（会话、消息、上传文档记录），**绝不存向量**；向量归 Chroma。零第三方依赖，纯 `sqlite3` 标准库。

**核心函数清单**（本文件无 class）：
`_connect()`、`init_db()`、`create_session()`、`get_all_sessions()`、`delete_session()`、`save_message()`、`get_session_messages()`、`add_uploaded_doc_meta()`、`get_uploaded_doc()`、`delete_uploaded_doc()`、`list_uploaded_docs()`

**逐个函数解析**：

- **`_connect()`**
  - 输入：无（路径读 `config.SQLITE_DB_PATH`）。
  - 逻辑：`sqlite3.connect(timeout=15)` —— FastAPI 同步接口跑在线程池，并发写时锁等待最多 15 秒而不是立刻抛 `database is locked`；`row_factory = sqlite3.Row` 返回类字典行；`PRAGMA foreign_keys=ON`（SQLite 默认**不**开外键，必须每连接显式开启）；`PRAGMA journal_mode=WAL` 减少读写互斥。
  - 返回：`sqlite3.Connection`。
  - 链路作用：所有 CRUD 的连接工厂，每个操作短连接、`with conn` 退出时自动 commit/关闭。

- **`init_db()`**
  - 输入：无。逻辑：先 `os.makedirs` 确保 db 目录存在；`executescript` 幂等建三张表（`CREATE TABLE IF NOT EXISTS`）；随后做**旧库迁移**——`PRAGMA table_info(messages)` 检查列集合，没有 `meta` 列就 `ALTER TABLE ADD COLUMN meta TEXT`。
  - 表结构：
    - `sessions(session_id PK, session_name, created_at)`
    - `messages(id 自增 PK, session_id FK→sessions, role, content, meta[JSON], created_at)`
    - `uploaded_docs(doc_id PK, file_name, chunk_count, upload_time)`
  - 链路作用：api.py `startup` 事件调用，服务启动即就绪。

- **`create_session(session_name="新会话")`**
  - 逻辑：`uuid4().hex` 生成 sid，插入 sessions。返回 sid。
  - 链路作用：对应 `POST /api/session`。

- **`get_all_sessions()`**
  - 逻辑：`SELECT * ORDER BY created_at DESC`，Row 转 dict 列表。
  - 链路作用：会话列表接口；chat 接口也用它做 session 存在性校验（外键兜底前的显式 404）。

- **`delete_session(session_id)`**
  - 逻辑：**先删 messages 再删 sessions**——messages 表外键未声明 `ON DELETE CASCADE`，直接删会话会因外键约束报错；用 `cur.rowcount > 0` 判断会话是否真实存在并返回布尔值。
  - 链路作用：对应 `DELETE /api/session/{session_id}`。

- **`save_message(session_id, role, content, meta=None)`**
  - 逻辑：插入一条消息；`meta` 非空时 `json.dumps(ensure_ascii=False)` 序列化（中文不转义），为空存 NULL。
  - 链路作用：每次问答后由 api.py 调两次：user 消息（无 meta）、assistant 消息（meta 含 sources/confidence/rewritten_query/sub_queries/rerank_scores），使前端刷新历史后来源和调试面板仍可渲染。

- **`get_session_messages(session_id)`**
  - 逻辑：按 `id ASC`（对话时间序）查全部消息；逐条把 meta 列 `json.loads` 反序列化，解析失败 warning 并 pop 掉 meta 键；**meta 为 NULL 时也 pop 键**（注释说明：避免前端用 `"meta" in msg` 误判出空面板）。
  - 链路作用：历史接口 + chat 时构建 `chat_history` 供多轮改写。

- **`add_uploaded_doc_meta(file_name, chunk_count, doc_id=None)`** / **`list_uploaded_docs()`**
  - 逻辑：前者插入上传记录并返回 doc_id；`doc_id` 可由调用方（api.py）**预先生成传入**——上传流程在写入 Chroma 前就需要 doc_id 注入每个 chunk 的 metadata，以此建立「SQLite 文档记录 ↔ Chroma 向量」的关联；不传则内部 `uuid4().hex` 生成。后者按 upload_time 倒序返回全部记录。
  - 链路作用：文档上传成功后登记元数据；前端「已上传文档」列表展示与删除。

- **`get_uploaded_doc(doc_id)`** / **`delete_uploaded_doc(doc_id)`**
  - 逻辑：前者按主键查单条文档记录（不存在返回 None）；后者按主键删除记录，`rowcount` 判断是否真实删除。
  - 链路作用：`DELETE /api/doc/{doc_id}` 接口——先查记录拿到 file_name（作为 Chroma 删除的 source 兜底匹配键），Chroma 向量删除后再删 SQLite 记录。

**关键细节与坑点**：
- 外键约束是每连接 `PRAGMA foreign_keys=ON` 才生效，忘开则孤儿消息可写入。
- `meta` 列是后期通过 ALTER 在线迁移加上的，兼容旧库。
- 每次调用都新建连接（无连接池），WAL 模式下短连接并发写可接受，但高并发下有 connect 开销。

---

### 2. utils/file_loader.py

**文件定位**：文档入库链路的最前端——把磁盘文件解析成 LangChain `Document` 列表（携带 `source` 元数据）。api.py 上传接口与 main.py 的 `process_paths` 都经此入口。

**核心函数清单**：
`load_file(path)`、`load_files(paths)`、`_read_file(path)`（私有分发器）
模块常量：`SUPPORTED_EXTS = {".txt", ".md", ".pdf", ".json", ".csv"}`、`_ENCODINGS = ("utf-8", "gbk", "latin-1")`

**逐个函数解析**：

- **`load_file(path)`**
  - 输入：文件路径（str 或 Path）。
  - 逻辑：① 后缀小写化后不在白名单 → warning 返回 None；② 调 `_read_file` 解析；③ **空文档过滤**：保留 `page_content` 非空白的 Document，全空（如扫描版 PDF）返回 None；④ 任何异常 catch 后记 error 返回 None —— 单文件失败不影响批量。
  - 返回：`Optional[List[Document]]`（PDF 一页一个 Document）。
  - 链路作用：入库链路「解析」一步。

- **`load_files(paths)`**
  - 逻辑：循环调 `load_file`，把非 None 结果**平铺 extend** 成一个大列表。
  - 返回：`List[Document]`。
  - 链路作用：api.py 上传临时文件、main.py `process_paths` 的批量入口。

- **`_read_file(path)`**（按后缀分发具体 Loader）
  - `.pdf`：`PyPDFLoader(path).load()`；若所有页文本都为空，warning「PDF 可能是扫描图片版」（不报错，返回空内容列表由上层过滤）。
  - `.txt / .md`：按 utf-8 → gbk → latin-1 顺序尝试 `TextLoader(encoding=...)`，捕获 `UnicodeDecodeError` 继续下一个；三种编码全失败时用 `path.read_text(encoding="latin-1")` 手工构造 Document 兜底（latin-1 单字节映射永不抛解码异常，保证不炸）。
  - `.json`：`JSONLoader(jq_schema=".", text_content=False)` —— 把整个 JSON 内容作为一条记录加载。
  - `.csv`：`CSVLoader(path)` —— 每行一个 Document。
  - 其他后缀：抛 `ValueError`（理论不可达，上层已拦截）。

**关键细节与坑点**：
- `JSONLoader` 依赖第三方 `jq` 包（langchain_community 在模块顶部 import jq）；requirements.txt 已声明 `jq>=1.7` 并注释说明 Windows 上可能需要编译环境，仅用 txt/md/pdf 时可不装。
- `PyPDFLoader` 实际依赖 `pypdf` 包（PyPDF2 的继任者），requirements.txt 已直接声明 `pypdf>=4.0`（旧版写 PyPDF2 的错配已修复）。
- 编码兜底链中 latin-1 不会失败，因此最后的手工兜底分支实际上几乎不可达，属于双保险。

---

### 3. utils/logger.py

**文件定位**：全项目统一日志工厂，各模块 `setup_logger("xxx", config.LOG_FILE)` 取 logger。

**核心函数清单**：`setup_logger(name="rag", log_file=None, level=INFO)`

**函数解析**：
- 输入：logger 名、可选落盘路径、日志级别。
- 逻辑：
  1. `logging.getLogger(name)`，若该 logger **已有 handler 直接返回**（防止模块多次 import/调用导致重复 handler、日志刷屏）；
  2. 统一格式 `时间 [级别] 名称: 消息`；
  3. **Windows 专项兜底**：`sys.platform == "win32"` 时把 `sys.stdout/stderr` `reconfigure(encoding="utf-8", errors="replace")`——Windows 控制台默认 GBK，中文/emoji 日志会抛 UnicodeEncodeError 把 uvicorn 炸栈；reconfigure 失败（非文本流）静默忽略；
  4. 添加 `StreamHandler(sys.stdout)`；
  5. `log_file` 非空时 `os.makedirs` 建目录后加 `FileHandler(encoding="utf-8")`；
  6. `logger.propagate = False` 阻止向 root logger 传播导致重复打印。
- 返回：配置好的 logger 实例。
- 链路作用：横切组件，被所有业务模块使用。

**关键细节**：handler 幂等检查 + propagate=False 共同保证多模块共享 logging 体系时不重复输出。

---

### 4. utils/reranker_helper.py

**文件定位**：检索增强的无状态工具模块——Multi-Query 子查询生成（检索前扩召回）和文档重排（检索后精排）。重排采用**三级降级**策略：本地 sentence-transformers CrossEncoder 优先（无网络依赖、无 API 配额与费用，模型单例缓存），本地模型未安装/加载失败/推理失败时降级硅基流动 `/rerank` HTTP API，两级均失败再返回原顺序。重型依赖 torch/sentence-transformers 全部函数内懒加载，任何失败都不阻断主链路。

**核心函数清单**：`multi_query_generate(original_query, llm, num_queries=3)`、`rerank_documents(query, docs, top_n, model_name=...)`、`_rerank_via_api(...)`（私有）、`_rerank_local(...)`（私有）

**逐个函数解析**：

- **`multi_query_generate(original_query, llm, num_queries=3)`**
  - 输入：原始问题、LangChain ChatModel 实例、子查询数量（默认 3，实际由 `config.MULTI_QUERY_NUM` 传入）。
  - 逻辑：构造提示词（要求从不同角度生成子查询、每行一个、不要编号和解释）→ `llm.invoke(prompt)` → 按行切分、去空行 → **过滤掉以数字或 `-`/`*`/`·` 开头的行**（LLM 不听话加编号/列表符时清洗）→ 保底合并：`[原始问题] + 清洗结果 + [原始问题]*num`，用 `dict.fromkeys` 保序去重后截取前 `num_queries` 个 → 结果第一个必然是原始问题。
  - 返回：子查询列表；任何异常 warning 后回退 `[original_query]`。
  - 链路作用：LangGraph `multi_query_node` 调用，产出喂给 `Retriever.retrieve_multi`。
  - 注意：由于 `[原问题] + N条生成` 再截取前 num_queries 条，LLM 生成的子查询实际最多只有 `num_queries - 1` 条被使用。

- **`rerank_documents(query, docs, top_n, model_name="BAAI/bge-reranker-v2-m3")`**
  - 输入：查询、召回 Document 列表、精排返回数（调用方传 `config.top_k`）、模型名。
  - 逻辑：空列表直接返回；先调 `_rerank_local`，返回非 None 即采用；否则降级 `_rerank_via_api`；两级都为 None 时 warning 并返回 `docs[:top_n]` 原顺序。
  - 链路作用：LangGraph `rerank_node` 调用，把多路召回的粗排结果精排为 top_k。

- **`_rerank_via_api(query, docs, top_n, model_name)`**（硅基流动云重排，第二级兜底）
  - 前置条件：`config.OPENAI_API_KEY` 有效（非空且不以 `your_` 开头），否则直接返回 None。
  - 候选模型：`list(dict.fromkeys([model_name, "BAAI/bge-reranker-v2-m3"]))`——配置模型名与平台兜底模型**保序去重**后逐个尝试，规避「配置了平台不存在的模型名」导致整体失败（早期实现曾用 `candidates.count()` 做去重判断，当配置名恰为兜底模型时两次循环都被跳过、API 完全不执行，已修复）。
  - 请求：`POST {OPENAI_API_BASE}/rerank`，Bearer 鉴权，body 为 `{model, query, documents}`，**不传 top_n**——请求全量打分、本地再截取，保证打分覆盖全部候选；timeout=30s。
  - 响应处理：非 200 / JSON 解析失败 / 无 `results` 字段均记 `last_err` 并尝试下一候选；成功后构建 `index → relevance_score` 映射，按分数降序排列原文档下标，缺失分数按 0.0 处理，截取 top_n，分数 `round(4)` 写回 `doc.metadata["rerank_score"]`（供前端调试面板）。
  - 返回：重排后 Document 列表；全部候选失败返回 None 交由上层降级到原顺序。

- **`_rerank_local(query, docs, top_n, model_name)`**（本地 CrossEncoder，首选路径）
  - 逻辑：先取模型单例 `_get_local_reranker(model_name)`（模型不可用返回 None → 上层降级云端）；成功后函数内 `import numpy`，构造 `(query, doc.page_content)` 配对 → `model.predict(pairs)` 打分 → **形状防御**：`np.asarray(scores).reshape(-1).tolist()` 拍平（部分模型返回 (n,1) 二维数组或标量，不拍平会导致 zip/sort/float 崩溃）→ 按分数降序排序 → 分数 `round(4)` 写回 metadata → 返回前 top_n 个。
  - 返回：重排后的 Document 列表；模型不可用或执行异常均返回 None，由上层降级云端 API。

**关键细节与坑点**：
- **本地 CrossEncoder 模型为进程内单例**（模块级 `_local_model_cache` + 双检锁），只加载一次，避免每次问答重复实例化（含首次联网下载）；**加载失败会记入 `_local_model_failed`**，后续请求直接跳过本地、走云端兜底，避免每次问答都重试一次失败的加载而阻塞请求（重启进程可重置，便于装好依赖后重新启用本地优先）。
- **本地优先的代价**：首次问答若本地未缓存该模型，会触发一次联网下载（耗时明显）；如需完全离线，应预下载模型并设置 `HF_HUB_OFFLINE=1`，否则会自动降级到云端 API。
- 中文场景默认模型为 `BAAI/bge-reranker-v2-m3`：本地路径需该模型已在本地缓存，云端路径由平台提供。
- 降级策略一致：无密钥/请求失败/依赖缺失/推理失败都逐级降级，链路不断。
- chromadb 的 `$contains` 操作符仅用于列表字段、不支持字符串子串匹配（文档删除的 source 兜底匹配踩过此坑，改为 Python 端过滤）。

---

### 5. config.py

**文件定位**：全项目唯一配置中心。模块导入时即 `load_dotenv(ROOT_DIR/.env)`，所有常量 `os.getenv` 读取并带默认值——**没有 .env 也能以降级模式启动**。

**核心内容清单**（无函数，纯模块级常量）：

| 常量 | 默认值 | 含义 |
|---|---|---|
| `ROOT_DIR` | config.py 所在目录绝对路径 | 锚定项目根，避免依赖进程 CWD |
| `CHROMA_DB_PATH` | `ROOT_DIR/db/chroma_db` | Chroma 持久化目录 |
| `SQLITE_DB_PATH` | `ROOT_DIR/db/rag.db` | SQLite 库文件 |
| `LOG_FILE` | `ROOT_DIR/logs/rag.log` | 日志落盘路径 |
| `MAX_UPLOAD_MB` | 10 | 上传大小上限（api.py 413 校验） |
| `CHUNK_SIZE` / `CHUNK_OVERLAP` | 500 / 100 | 切片参数（前端滑块可临时覆盖） |
| `TOP_K` | 3 | 检索/重排返回数 |
| `MAX_RETRY` | 2 | 幻觉重试上限（防死循环） |
| `TEMPERATURE` / `MAX_TOKENS` | 0.1 / 1000 | 生成参数（**注意：当前代码未真正传给模型**，见第五部分） |
| `LLM_MODEL`（常量名 `DEEPSEEK_MODEL`） | `deepseek-ai/DeepSeek-V4-Flash` | 生成模型名 |
| `OPENAI_EMBEDDING_MODEL` | `BAAI/bge-m3` | Embedding 模型名（1024 维） |
| `RERANKER_MODEL_NAME` | `BAAI/bge-reranker-v2-m3` | 重排模型名（优先作为硅基流动 /rerank API 的模型名） |
| `MULTI_QUERY_NUM` | 3 | 子查询数量 |
| `OPENAI_API_BASE` | `https://api.siliconflow.cn/v1` | OpenAI 兼容端点（硅基流动），LLM/Embedding/Rerank 共用 |
| `DEEPSEEK_API_KEY` / `OPENAI_API_KEY` | 空字符串 | LLM 密钥 / Embedding+Rerank 密钥（硅基流动上两者通常相同） |
| `ENABLE_MULTI_QUERY` / `ENABLE_RERANKER` | True | 功能开关，识别 `1/true/yes` |

**关键细节**：
- LLM 和 Embedding 都走 OpenAI 兼容协议、同一个 base_url，两个 key 在硅基流动上通常填同一个值。
- 密钥判断逻辑统一为「非空且不以 `your_` 开头」（兼容 .env 模板占位符 `your_xxx_here`），不在源码里硬编码真实 key。
- 开关是模块级布尔，但前端可通过 chat 请求体**临时覆盖**（api.py 改单例字段后 finally 恢复）。

---

### 6. main.py（RAG 核心，项目最大文件）

**文件定位**：RAG 全部核心能力所在——Embedding 选择、文档处理、检索、生成、LangGraph 状态图编排。api.py 只做 HTTP 包装，真正的 RAG 链路都在这里。

**模块级组件**：
- `_model` 全局单例 + **`deepseek_model()`**：懒加载 LLM。无有效密钥抛 `ValueError`；有密钥则 `init_chat_model(model=config.DEEPSEEK_MODEL, model_provider="openai", base_url=config.OPENAI_API_BASE, api_key=config.DEEPSEEK_API_KEY)`。生成/改写/多查询/幻觉检测共用这一个实例。
- **`SimpleEmbeddings(Embeddings)`**：降级用伪 Embedding。`_embed_text` 对文本做 MD5，取 16 字节摘要循环映射到 384 维、归一化到 [-1,1]；同一文本向量确定，但**完全无语义**（近义词向量无关），仅用于无密钥时跑通流程，构造时会打生产警告。
- **`get_embeddings()`**：有有效 OPENAI_API_KEY → `OpenAIEmbeddings(model=bge-m3, base_url, api_key, timeout=30, check_embedding_ctx_length=False)`。两个关键参数：`timeout=30`（批量嵌入慢，10s 不够）；`check_embedding_ctx_length=False`——硅基流动等非 OpenAI 厂商不接受 langchain 默认 tokenize 分片后提交的 token 整数数组（会 400 The parameter is invalid），关闭后直接提交原始文本字符串。ImportError 或无密钥 → warning + `SimpleEmbeddings()`。

**核心类清单**：`RAGConfig`（dataclass）、`RAGState`（TypedDict）、`DocumentProcessor`、`Retriever`、`Generator`、`RAGChain`

#### 6.1 `RAGConfig`
- 定位：运行时配置对象，默认值全部取自 config 模块；dataclass 字段含 temperature、chunk_size/overlap、top_k、search_type（"similarity"/"mmr"）、max_tokens、max_retry、两个开关、multi_query_num、reranker_model_name。
- 细节：`temperature`/`max_tokens`/`search_type` 三个字段**当前是死配置**——init_chat_model 未传生成参数，检索也只调 similarity_search。

#### 6.2 `RAGState(TypedDict)`
- LangGraph 全程共享的状态字典：`query`、`chat_history`、`documents`、`context`、`answer`、`sources`、`confidence`，以及扩展字段 `sub_queries`、`rerank_scores`、`is_hallucination`、`retry_count`、`rewritten_query`、`hallucination_feedback`。

#### 6.3 `DocumentProcessor`（文档处理）
- **`__init__(config)`**：构造 `RecursiveCharacterTextSplitter`（chunk_size/overlap 来自配置，分隔符列表**中文友好**：`"\n\n","\n","。","！","？",".","!","?"," ",""`）；`self.embeddings = get_embeddings()`；调 `_create_vector_store()` 启动即建库。
- **`_create_vector_store()`**：try 中创建 `Chroma(collection_name="rag_docs", embedding_function, persist_directory=config.CHROMA_DB_PATH)`（Chroma 构造即加载已有持久化数据，不重建），随后调 `_check_embedding_compatibility()`；任何异常 → warning 并降级 `InMemoryVectorStore`（无持久化但流程可跑）。
- **`_check_embedding_compatibility()`**：维度自愈。取底层 `vector_store._collection`（chromadb Collection）；库为空直接返回；`get(include=["embeddings"], limit=1)` 取一条已有向量算 `stored_dim`，与 `embeddings.embed_query("维度探测")` 的 `current_dim` 比较；不一致（典型场景：先用 SimpleEmbeddings 384 维建库，后配 key 换 bge-m3 1024 维）则**直接 `delete_collection` 后重建空 Chroma 实例**——注释解释了为什么只 `delete(ids)` 不够：collection 的 HNSW 维度元数据不随删数据更新，检索仍报 dimension 错。重建后需重新上传文档。整个自检包在 try/except 里，失败只 warning。
- **`rebuild_text_splitter()`**：按当前 config 重建 splitter。前端改 chunk 参数后必须调（splitter 在 __init__ 时固化了旧值）。
- **`split_documents(documents)`**：委托 splitter 切分。
- **`create_vector_store(documents)`**：`vector_store.add_documents(documents)` **追加**写入（不新建 Chroma 实例，防重复实例化）。
- **`load_documents(texts, metadatas)` / `process(texts, metadatas)`**：从纯文本构造 Document 并走「加载→切片→写入」全流程（早期 Demo 入口保留）。
- **`process_paths(paths) -> int`**：文件路径入口：`file_loader.load_files` → `split_documents` → `create_vector_store`，返回 chunk 数。
- **`delete_documents_by_doc_id(doc_id, source=None) -> int`**：从 Chroma 删除指定文档的全部 chunk，返回删除数量。**两级匹配**兼容历史数据：① 优先 `store.get(where={"doc_id": doc_id})` 精确匹配（新上传文档在入库时已注入 doc_id）；② 未命中且传入 source（文件名）时，`store.get(include=["metadatas"])` 拉取全部元数据在 **Python 端做子串过滤**（注意 chromadb 的 `$contains` 仅支持列表字段、不支持字符串子串匹配，不能用于 where 过滤），兼容早期未注入 doc_id 的历史 chunk。匹配到 id 后 `store.delete(ids=...)`；向量库为内存兜底或异常时 warning 返回 0。
- 链路作用：入库链路的切片/向量化/存储环节，以及文档删除链路的向量清理环节。

#### 6.4 `Retriever`（检索）
- **`__init__(vector_store, config)`**：持有向量库与配置。
- **`retrieve(query)`**：单路 `similarity_search(query, k=top_k)`。
- **`retrieve_multi(queries)`**：多路召回。逐条子查询 `similarity_search(k=top_k)`，按 `page_content` **精确文本去重**合并；候选数达到 `top_k * 3` 提前 break（少发向量库请求）。
- **`retrieve_with_scores(query)`**：`similarity_search_with_score` 返回 (Document, score) 元组——**当前无调用方（死代码）**；源码注释提醒：Chroma 返回的是**距离值不是 0~1 相似度**，不能当概率用。
- 链路作用：问答链路「召回」环节。

#### 6.5 `Generator`（生成）
- **`__init__(config)`**：`self.llm = None`（懒加载）；内置三套 Prompt 模板：
  - `rag_prompt`：系统角色限定「只用上下文回答、无信息就明说无法回答、末尾标注来源」，`MessagesPlaceholder(chat_history, optional=True)` + 人类 query；
  - `rewrite_prompt`：结合历史把问题改写成独立完整查询，只返回改写结果；
  - `hallucination_prompt`：要求只输出 JSON `{"is_hallucination": true/false, "reason": ...}`。
- **`_llm_available()`**：密钥有效判定（同上 `your_` 前缀规则）。
- **`_get_llm()`**：无密钥抛 RuntimeError；有则懒加载 `deepseek_model()` 单例。
- **`rewrite_query(query, chat_history)`**：无历史或无 LLM → 原句返回；否则取**最近 4 轮**历史转 Human/AI Message，`rewrite_prompt | llm | StrOutputParser` 链调用。
- **`generate(query, context, chat_history=None, feedback=None)`**：无 LLM → 降级文案（明确提示演示模式 + 透出检索到的 context 前 600 字）；有 LLM 则拼最近 4 轮历史；**`feedback` 非空时追加一条 SystemMessage**——幻觉重试时注入纠偏要求（「上一版被驳回、只许用上下文、没有就明说」），解决「相同输入重试等于空转、只靠随机采样」的问题；最后 `rag_prompt | llm | StrOutputParser` 生成。
- **`evaluate_confidence(query, context, answer)`**：无 LLM 返回 0.5；有 LLM 走评分 Prompt（要求只返回 0~1 数字）；解析时先 `re.sub(r"<[/]?[^>]+>", "", raw)` 剥离推理模型的 `<reasoning>` 思考块标签（DeepSeek 推理模型带思考块会导致 `float()` 恒异常、置信度恒 0.5），再正则提取第一个数字，clamp 到 [0,1]；任何异常回退 0.5。
- **`detect_hallucination(query, context, answer)`**：空回答或无 LLM → False；调幻觉 Prompt 后把结果转小写，用**字符串匹配**判定：包含 `"is_hallucination": true` 或以 `true` 开头 → True；异常保守返回 False（不触发无谓重试）。
- 链路作用：问答链路的改写、生成、自评、反思四个环节。

#### 6.6 `RAGChain`（LangGraph 编排）
- **`__init__(config=None)`**：组装 `DocumentProcessor`、`Retriever`（绑定 processor 的 vector_store）、`Generator`；`graph = None`（建图延迟到 `index_documents`）。
- **`index_documents(texts, metadatas=None)`**：智能入口——若入参全是以 `.txt/.md/.pdf/.json/.csv` 结尾的字符串，视为**文件路径**走 `processor.process_paths`；否则视为**纯文本**走 `processor.process`；之后**重新关联 retriever**（指向最新 vector_store）并 `_build_graph()`。api.py 启动时用空列表调它来完成建图。
- **`_build_graph()`**：定义 7 个节点闭包 + 1 个内部辅助函数，然后构图编译：
  - `_format_context_and_sources(docs)`：把文档列表拼成 `[文档 i] 内容` 的 context 串，同时产出 sources（序号、source 元数据、前 100 字预览）。
  - `process_query`：有历史时调 `rewrite_query`（try/except 失败用原句），写回 `query` 和 `rewritten_query`。
  - `multi_query_node`：默认 `sub_queries=[query]`；开关开时调 `multi_query_generate(query, generator.llm or generator._get_llm(), multi_query_num)`，异常回退原查询。
  - `retrieve_documents`：开关开 → `retrieve_multi(sub_queries)`，否则 `retrieve(query)`；**检索异常被 catch 后记 error 返回空列表**（这就是维度不匹配时「静默什么都搜不到」的根源，所以才有启动维度自检）；随后格式化 context/sources。
  - `rerank_node`：开关开且有文档 → `rerank_documents(query, docs, top_k, reranker_model_name)`，用精排结果替换 documents，重写 rerank_scores/context/sources；异常保持原顺序。
  - `generate_answer`：调 `generator.generate(..., feedback=state.hallucination_feedback)`；异常兜底固定文案「生成回答时发生错误」，保证节点不炸。
  - `hallucination_check_node`：`retry_count >= max_retry` → **强制 `is_hallucination=False` 放行**（防死循环）；否则调 `detect_hallucination`；为 True 时 `retry_count+1` 并写入 `hallucination_feedback` 纠偏文本。
  - `evaluate_response`：调 `evaluate_confidence` 写 state。
  - 图边：`START→process_query→multi_query_node→retrieve→rerank_node→generate→hallucination_check_node`；条件边 `route_after_hallucination`：True → 回 `generate`，False → `evaluate→END`。
- **`query(question, chat_history=None)`**：初始化全字段 state（retry_count=0 等）→ `graph.invoke(initial_state)` → 返回 `{answer, sources, confidence, sub_queries, rewritten_query, rerank_scores}`。
- 链路作用：问答链路的总编排器。

**关键细节与坑点**：
- 幻觉重试闭环：generate ↔ hallucination_check 之间是**环**，靠 retry_count 计数 + max_retry 强制跳出；重试不改上下文，靠 feedback SystemMessage 改变生成行为。
- 幻觉判定是子串匹配，LLM 输出 JSON 带空格/代码块/中文键时可能漏判（漏判=放行，偏保守）。
- 置信度是 LLM 自评，不是检索相似度。
- 维度自愈会 drop 整个 collection：向量是派生数据可重建，但 SQLite 里 uploaded_docs 记录仍在，会出现「文档列表有、向量库没有」的不一致（此时可在前端删除该文档记录后重新上传，删除接口的 source 兜底匹配也能清掉残留向量）。

---

### 7. api.py

**文件定位**：FastAPI 服务层，把 RAG 核心能力暴露为 REST 接口，并负责 SQLite 读写、单例管理、并发保护。前端只与本文件对话。

**核心对象/函数清单**：
- 全局：`app`、`_rag`（RAGChain 单例）、`_config_lock`（线程锁）
- `get_rag()`、`startup()`
- Pydantic 模型：`ChatRequest`、`CreateSessionRequest`、`ChatResponse`
- 路由：`health()`、`upload_doc(file)`、`list_docs()`、`delete_doc(doc_id)`、`chat(req)`、`create_session(req)`、`list_sessions()`、`session_history(session_id)`、`delete_session(session_id)`

**逐个解析**：

- **`get_rag()`**：双检锁（DCL）单例。`_rag is None` 时进锁、锁内二次判空，构造 `RAGConfig()` + `RAGChain(cfg)`，并调 `index_documents([])` 完成建图（Chroma 同时加载已有持久化数据）。防止并发首请求各自构造一套 Chroma/LLM 并互相覆写。
- **`startup()`**（FastAPI startup 事件）：`sqlite_db.init_db()` 建表 + `get_rag()` 预热。
- **`health()`**：`GET /health` 返回 `{"status":"ok"}`，前端探活用。
- **`upload_doc(file)`**（`POST /api/doc/upload`，async）：
  1. filename 为空兜底 `"unnamed"`；
  2. `await file.read()` 后按 `MAX_UPLOAD_MB`（默认 10MB）校验，超限抛 413；
  3. 后缀白名单 `{.txt,.md,.pdf,.json,.csv}`，不符抛 400；
  4. 临时文件写到 `ROOT_DIR/tmp/`（**固定锚定项目根**，不依赖 CWD），文件名加 `os.urandom(4).hex()` 随机前缀防并发冲突；
  5. `file_loader.load_files([fpath])` 解析，`finally` 里 `unlink(missing_ok=True)` 用完即删；
  6. 解析为空 → 400；
  7. **锁内**执行状态变更：先 `uuid4().hex` **预生成 doc_id**，`split_documents` 后把 doc_id 注入**每个 chunk 的 `metadata["doc_id"]`**（建立 SQLite 文档 ↔ Chroma 向量的关联，删除时据此精准定位），再 `create_vector_store` 追加向量 → 重建 `Retriever` 关联 → `_build_graph()` 重建图；
  8. `sqlite_db.add_uploaded_doc_meta(filename, chunk_count, doc_id=doc_id)` 登记（复用同一 doc_id）；返回 doc_id/file_name/chunk_count。
- **`list_docs()`**（`GET /api/doc/list`）：返回 SQLite 文档元数据列表。
- **`delete_doc(doc_id)`**（`DELETE /api/doc/{doc_id}`）：
  1. `sqlite_db.get_uploaded_doc(doc_id)` 查记录，不存在抛 404；
  2. **锁内**调 `processor.delete_documents_by_doc_id(doc_id, source=doc["file_name"])`——doc_id 精确匹配优先、文件名 source 兜底（兼容历史 chunk）；
  3. 随后 `sqlite_db.delete_uploaded_doc(doc_id)` 删元数据；**向量与元数据分两步互不阻塞**：即使 Chroma 删除异常（返回 0），元数据也要清掉，避免残留死记录；
  4. 返回 doc_id/file_name/deleted_chunks。
- **`chat(req)`**（`POST /api/chat`，同步函数跑在线程池）：
  1. 取单例 rag；
  2. 会话存在性校验（遍历 sessions），不存在抛 404（外键开启后直接插会 IntegrityError，提前显式报错）；
  3. **锁内**做「快照 → 覆盖 → 查询 → finally 恢复」：快照 5 个运行值（processor 的 chunk_size/overlap、retriever 的 top_k、两个开关）；按请求体非空字段覆盖；chunk 参数变化时调 `rebuild_text_splitter()`（否则改 config 不生效）；从 SQLite 拉该会话全部历史构建 `chat_history`；调 `rag.query(question, chat_history)`；`ValueError` 转 500；
  4. `finally` 恢复 5 个快照值，若重建过 splitter 则再重建一次保持一致——**保证一个会话的临时参数不泄漏给下一个会话**；
  5. 锁外保存消息：user 消息无 meta；assistant 消息带 meta（sources/confidence/rewritten_query/sub_queries/rerank_scores）；
  6. 返回 answer/sources/confidence 等字段。
- **`create_session(req)`**（`POST /api/session`）：调 SQLite 建会话返回 sid。
- **`list_sessions()`**（`GET /api/session`）：返回全部会话。
- **`session_history(session_id)`**（`GET /api/session/{id}/history`）：返回消息列表；会话不存在（无消息且 sessions 表无记录）抛 404。
- **`delete_session(session_id)`**（`DELETE /api/session/{session_id}`）：调 `sqlite_db.delete_session`（内部先删消息再删会话），返回 `{session_id, deleted: true}`；会话不存在抛 404。

**关键细节与坑点**：
- `_config_lock` 同时保护「上传导致的单例状态变更」和「chat 的参数覆盖」，避免并发竞态；但代价是**所有问答请求被全局串行化**（含 LLM 网络等待），吞吐受限。
- 参数覆盖基于可变单例 + 快照恢复，设计脆弱：任何新增可覆盖参数都必须手动加进快照/恢复元组，漏一个就跨请求泄漏。
- 上传的切片/入库逻辑没复用 `RAGChain.index_documents`（因为需要 chunk 数且文件已是 Document），而是直接调 processor 方法后手动重建 retriever/graph——与 index_documents 存在重复逻辑。
- 临时文件名是 `随机前缀_原始filename`，filename 未做路径字符清洗（低危路径穿越隐患，正常客户端不会构造）。

---

### 8. frontend.py

**文件定位**：Streamlit 前端，**纯 HTTP 客户端**——全文件不 import main/RAGChain，只通过 requests 调 FastAPI，实现彻底前后端分离。

**核心函数清单**：`api(method, path, **kwargs)`、`load_history(session_id)`；其余为 Streamlit 脚本式页面代码（侧边栏 + 主区）。

**函数解析**：
- **`api(method, path, **kwargs)`**：拼 `API_BASE + path`，`requests.request(timeout=120)`；成功返回 `(status_code, r.json())`；JSON 解析失败返回 `(code, {"text": r.text})`；网络异常返回 `(None, {"error": str(e)})`。统一错误形态，页面代码只需判 code。
- **`load_history(session_id)`**：GET 历史接口，200 取 messages，否则空列表。

**页面结构与逻辑**：
- 侧边栏：
  1. **服务状态**：API 地址输入框（可改，改后 rerun）；GET /health 探活，失败 `st.stop()` 阻断页面；
  2. **文档上传**：`file_uploader` 支持 .pdf/.txt/.md/.json/.csv 多选；「上传并索引」按钮逐个 POST multipart，按 code 显示成功 chunk 数或错误；**文档列表常驻展示**（GET /api/doc/list），每行两列：文件名+chunks 数、🗑️ 删除按钮（DELETE /api/doc/{doc_id}，成功后 rerun 刷新列表）；
  3. **RAG 参数面板**：chunk_size 滑块（200–1000，步 100，默认 500）、chunk_overlap（0–200，步 50，默认 100）、top_k（1–8，默认 3）、Multi-Query / Reranker 两个 toggle（默认开）；
  4. **会话区**（标题写的是「会话语义」，应为「会话管理」的笔误）：新建会话按钮（POST 后写入 session_state 并 rerun）、会话下拉选择（format_func 显示 session_name）、「🗑️ 删除当前会话」按钮（DELETE /api/session/{sid}，成功后清空 session_state 中的 session_id/history/loaded_sid 并 rerun，避免引用已删除会话）。
- 主区：
  - 标题「RAG 智能问答」；
  - session_state 维护 history；切换会话（sid 变化）时自动 `load_history`；
  - 渲染对话气泡（`st.chat_message`，用户头像 🧑 / 助手头像 🤖）；assistant 消息带 meta 时渲染两个折叠面板：「📎 来源与置信度」（来源序号加粗 `[i]` + source 文件名 + 内容预览 caption + 加粗置信度）、「🔍 调试信息」（改写后 query、**Multi-Query 子查询与 Rerank 打分用 `st.json()` 渲染为语法高亮的可折叠 JSON 树**）；
  - `st.chat_input` 发送：先把用户消息乐观追加到界面，POST /api/chat（payload 带 sid、question 和**全部 5 个参数面板当前值**）；200 则追加 assistant 消息（含 meta）；非 200 显示错误并追加一条错误气泡；最后 rerun；
  - 未选会话就提问 → warning 提示先建会话。

**关键细节**：
- 前端每次提问都把滑块/开关当前值随请求发出（即使是默认值），后端通过「值是否变化」判断是否重建 splitter，默认值不会触发多余重建。
- 历史消息的来源面板依赖后端把 meta 持久化到 SQLite（assistant 消息 meta 字段）。
- Streamlit 是服务端发 requests 到 FastAPI，所以不存在浏览器 CORS 问题。

---

### 9. smoke_api_test.py

**文件定位**：一次性冒烟测试脚本（文件头注释「完成后可删除」），用 `fastapi.testclient.TestClient` 在进程内串测接口主流程，无需起服务。

**逻辑（无函数，顺序脚本）**：
1. `TestClient(api.app)` —— 构造即触发 startup（建表 + 预热 RAG 单例）；
2. GET /health；
3. GET /api/session 看会话数；
4. POST /api/session 建 smoke 会话，取 sid；
5. POST /api/doc/upload 上传一段关于 LangChain/Chroma 的中文 `sample.txt`；
6. GET /api/doc/list；
7. POST /api/chat 问「什么是LangChain？」（无密钥走降级模式），打印 answer 头部、confidence、sub_queries；
8. GET 历史，断言应有 2 条消息；
9. 全部通过打印 `=== ALL API ENDPOINTS PASSED ===`。

**关键细节**：验证的是「接口连通 + 降级模式全流程可跑」；不校验回答质量，也未覆盖两个 DELETE 接口（删除链路由手工/独立脚本验证）。上传用 txt 避开了 pypdf/jq 等可选依赖，保证最干净环境也能跑过。

---

## 三、完整业务执行链路

### 3.1 文档入库链路

```
用户在 frontend.py 侧边栏选择文件 → 点击「上传并索引」
  │  frontend.py: api("POST", "/api/doc/upload", files={"file": (name, bytes, type)})
  ▼
api.py: upload_doc(file)
  1. file.filename 兜底 "unnamed"                                   [api.py:101]
  2. await file.read() 读全量 → 大小 > MAX_UPLOAD_MB(10MB) → 413    [api.py:103-106]
  3. Path(filename).suffix 白名单校验 {txt,md,pdf,json,csv} → 400   [api.py:109-111]
  4. 写临时文件 ROOT_DIR/tmp/<随机hex>_<filename>                    [api.py:115-118]
  ▼
utils/file_loader.py: load_files([fpath]) → load_file(path)
  5. 后缀白名单二次校验                                             [file_loader.py:21]
  6. _read_file(path) 按后缀分发：                                  [file_loader.py:49-90]
       .pdf  → PyPDFLoader().load()（一页一 Document，扫描版告警）
       .txt/.md → TextLoader 编码链 utf-8→gbk→latin-1
       .json → JSONLoader(jq_schema=".", text_content=False)
       .csv  → CSVLoader（一行一 Document）
  7. 过滤全空白 Document；异常/空文件 → None（api.py 随后 400）
  ▼
api.py（finally 中删除临时文件）                                     [api.py:122-123]
  8. 锁内：                                                         [api.py:131-142]
       doc_id = uuid4().hex  # 预生成，Chroma 与 SQLite 共用同一 id
       rag.processor.split_documents(docs)
         → main.py DocumentProcessor.split_documents
         → RecursiveCharacterTextSplitter（chunk_size=500/overlap=100，中文分隔符）
       每个 chunk 的 metadata 注入 doc_id（建立 SQLite 记录 ↔ Chroma 向量关联）
       rag.processor.create_vector_store(chunks)
         → main.py: Chroma.add_documents(chunks)
           （内部对每个 chunk 调 embeddings.embed_documents：
            有 OPENAI_API_KEY → bge-m3 远程嵌入；否则 SimpleEmbeddings 384 维哈希向量；
            向量+文本+metadata(含 doc_id/source) 持久化到 db/chroma_db 的 rag_docs collection）
       rag.retriever = Retriever(rag.processor.vector_store, rag.config)  # 重关联
       rag._build_graph()                                                # 重建状态图
  9. sqlite_db.add_uploaded_doc_meta(filename, len(chunks), doc_id=doc_id)
       → db/rag.db 的 uploaded_docs 表插一条元数据（复用同一 doc_id）  [sqlite_db.py:157]
  ▼
返回 {doc_id, file_name, chunk_count} → 前端显示「✅ xxx：N 个文本块」
```

### 3.2 文档/会话删除链路

```
文档删除（前端侧边栏 🗑️ 按钮）：
  frontend.py: api("DELETE", f"/api/doc/{doc_id}")
  ▼
api.py: delete_doc(doc_id)
  1. sqlite_db.get_uploaded_doc(doc_id) → 不存在 404
  2. 锁内：processor.delete_documents_by_doc_id(doc_id, source=file_name)
       ① Chroma where={"doc_id": doc_id} 精确匹配（新文档）
       ② 未命中 → 拉全部 metadata 在 Python 端按 source 包含 file_name 过滤（历史文档兜底）
       → store.delete(ids) 删除向量
  3. sqlite_db.delete_uploaded_doc(doc_id) 删元数据（向量删除失败也执行，避免死记录）
  ▼
返回 {doc_id, file_name, deleted_chunks} → 前端 rerun 刷新文档列表

会话删除（前端「🗑️ 删除当前会话」按钮）：
  frontend.py: api("DELETE", f"/api/session/{sid}")
  ▼
api.py: delete_session(session_id)
  → sqlite_db.delete_session：先 DELETE messages（外键无 CASCADE），再 DELETE sessions
  → 不存在 404；成功后前端清空 session_state（session_id/history/loaded_sid）并 rerun
```

### 3.3 用户问答链路

```
用户在 frontend.py 主区 chat_input 输入问题
  │  POST /api/chat  payload={session_id, question, chunk_size, chunk_overlap,
  │                           top_k, enable_multi_query, enable_reranker}
  ▼
api.py: chat(req)
  1. get_rag() 取单例（首请求双检锁构造 RAGChain + index_documents([]) 建图）
  2. 会话存在性校验 → 不存在 404
  3. 锁内：快照 5 个运行参数 → 按请求覆盖（chunk 变化时 rebuild_text_splitter）
  4. sqlite_db.get_session_messages(sid) → chat_history（全部历史消息）
  ▼
main.py: RAGChain.query(question, chat_history) → graph.invoke(initial_state)
  │
  ├─ 节点 process_query
  │    Generator.rewrite_query(query, 最近4轮历史)        [main.py:430]
  │    有历史：rewrite_prompt | LLM | StrOutputParser 把指代词改写为独立查询
  │    无历史/无密钥/异常：用原查询
  │
  ├─ 节点 multi_query_node
  │    开关开：reranker_helper.multi_query_generate(query, llm, 3)
  │      → LLM 生成多角度子查询，清洗编号，保序去重，首个恒为原查询
  │    开关关/异常：sub_queries = [原查询]
  │
  ├─ 节点 retrieve
  │    开关开：Retriever.retrieve_multi(sub_queries)
  │      → 每条子查询 Chroma.similarity_search(k=top_k)
  │      → page_content 精确去重合并，候选达 top_k*3 提前停止
  │    开关关：Retriever.retrieve(query) 单路检索
  │    异常 → 空文档列表（记 error）
  │    → _format_context_and_sources：拼 "[文档i] 内容" context + sources 列表
  │
  ├─ 节点 rerank_node
  │    开关开且有文档：reranker_helper.rerank_documents(query, docs, top_k, 模型名)
  │      → ① 本地 CrossEncoder（bge-reranker-v2-m3）对 (query, doc) 配对 predict 打分
  │      → ② 本地不可用/推理失败降级硅基流动 /rerank API 全量打分、本地截取
  │      → ③ 两级均失败保持召回原顺序
  │      → reshape 拍平（本地路径）→ 降序排序 → 分数写入 metadata["rerank_score"]
  │      → 返回 top_k；用精排结果重算 context/sources/rerank_scores
  │    失败/开关关：保持召回原顺序
  │
  ├─ 节点 generate
  │    Generator.generate(query, context, 最近4轮历史, feedback)
  │      无密钥：降级文案 + context 前 600 字
  │      有密钥：rag_prompt（只许用上下文+无信息明说+标注来源）
  │              | LLM(DeepSeek-V4-Flash via 硅基流动) | StrOutputParser
  │      重试轮次：feedback 以 SystemMessage 注入纠偏要求
  │      异常：固定错误文案，节点不中断
  │
  ├─ 节点 hallucination_check_node
  │    retry_count >= max_retry(2) → 强制 is_hallucination=False 放行
  │    否则 Generator.detect_hallucination(query, context, answer)
  │      → LLM 输出 JSON，子串匹配 "is_hallucination": true
  │    True  → retry_count+1，写 hallucination_feedback，条件边回 generate 重生成
  │    False → 条件边去 evaluate
  │
  └─ 节点 evaluate
       Generator.evaluate_confidence(query, context, answer)
         → LLM 打 0~1 分；剥离<reasoning>标签 → 正则取数 → clamp；异常/无密钥 0.5
  ▼
返回 {answer, sources, confidence, sub_queries, rewritten_query, rerank_scores}
  ▼
api.py: finally 恢复参数快照（+ 必要时重建 splitter）→ 释放锁
  5. sqlite_db.save_message(sid, "user", question)
  6. sqlite_db.save_message(sid, "assistant", answer, meta={sources, confidence,
              rewritten_query, sub_queries, rerank_scores})   # JSON 落库
  ▼
frontend.py 收到 200：assistant 气泡渲染回答
  + 「📎 来源与置信度」折叠面板（sources + confidence）
  + 「🔍 调试信息」折叠面板（改写 query / 子查询 / rerank 打分）
```

---

## 四、配置与部署文件解读

### 4.1 .env / .env.example

.env.example 是模板（复制为 .env 后填值），所有项与 config.py 一一对应，分四组：

| 分组 | 变量 | 作用 | 不填的后果 |
|---|---|---|---|
| 路径 | `CHROMA_DB_PATH` / `SQLITE_DB_PATH` / `LOG_FILE` | 向量库 / SQLite / 日志位置 | 用项目根下默认路径 |
| 路径 | `MAX_UPLOAD_MB=10` | 上传大小上限 | 10MB |
| 超参 | `CHUNK_SIZE=500` / `CHUNK_OVERLAP=100` | 切片大小/重叠 | 中文长文经验值 |
| 超参 | `TOP_K=3` | 召回与精排返回条数 | 3 |
| 超参 | `MAX_RETRY=2` | 幻觉重试上限（防死循环） | 2 |
| 超参 | `TEMPERATURE=0.1` / `MAX_TOKENS=1000` | 生成参数 | **当前未接线到模型，填了不生效** |
| 模型 | `DEEPSEEK_MODEL` | 生成模型（硅基流动上的 DeepSeek-V4-Flash） | 默认值可用 |
| 模型 | `OPENAI_EMBEDDING_MODEL=BAAI/bge-m3` | Embedding 模型（1024 维） | 默认值可用 |
| 模型 | `RERANKER_MODEL_NAME` | 重排模型（优先作为硅基流动 /rerank API 模型名，默认 BAAI/bge-reranker-v2-m3） | 默认值可用 |
| 模型 | `MULTI_QUERY_NUM=3` | 子查询数 | 3 |
| 密钥 | `OPENAI_API_BASE` | OpenAI 兼容端点（默认硅基流动，LLM/Embedding/Rerank 共用） | 默认值 |
| 密钥 | `DEEPSEEK_API_KEY` | LLM 密钥 | 降级模式：无真实回答，只回检索片段 |
| 密钥 | `OPENAI_API_KEY` | Embedding + Rerank API 密钥（硅基流动上与 DEEPSEEK_API_KEY 通常相同） | 降级 SimpleEmbeddings（检索无语义）+ 重排走本地模型 |
| 开关 | `ENABLE_MULTI_QUERY=1` / `ENABLE_RERANKER=1` | 两个增强功能开关 | 默认开；前端可临时覆盖 |

模板头部明确说明：不填密钥以「降级演示模式」运行，全流程（检索/来源/子查询/重排/幻觉校验）可跑通但质量不可用于生产。

### 4.2 requirements.txt

分组：基础（python-dotenv、pypdf、requests、python-multipart）、Web（fastapi、uvicorn、streamlit）、LangChain 全家桶（langchain/core/community/text-splitters/openai、langgraph，**已移除 langchain-groq**）、文档解析（jq，注释说明 Windows 编译问题）、向量库（langchain-chroma、chromadb）、重排（sentence-transformers，注释说明含 torch、安装失败程序自动降级）。版本均用 `>=` 主版本约束。

依赖声明已与代码对齐：`pypdf>=4.0`（PyPDFLoader 实际依赖，替代已停更的 PyPDF2）、`python-multipart>=0.0.9`（UploadFile 解析 multipart 必需）、`jq>=1.7`（JSONLoader 顶层依赖）均已补齐。

### 4.3 Dockerfile

- 基础镜像 `python:3.11-slim`；
- 先 `COPY requirements.txt` 再 pip 安装，利用 Docker 层缓存（改代码不重装依赖）；
- `COPY . .` 拷贝全部代码；`mkdir -p db/chroma_db` 预建持久化目录；
- `EXPOSE 8000 8501`；
- 启动命令 `sh -c "uvicorn api:app --host 0.0.0.0 --port 8000 & streamlit run frontend.py --server.port 8501 --server.address 0.0.0.0"`——uvicorn 后台、streamlit 前台，单容器双进程。

注意点：未挂 volume 时容器重建会丢 Chroma/SQLite 数据；uvicorn 后台崩溃不会导致容器退出（无进程看护）；streamlit 未显式加 `--server.headless=true`。

---

## 五、项目问题与优化建议

### 5.1 潜在缺陷与风险点（均有代码依据）

**A. 依赖与安装**（本组词条均已修复，保留存档）
1. ~~**缺 `python-multipart`**：FastAPI 的 `UploadFile/File` 依赖该包解析 multipart 表单，requirements.txt 未声明，干净环境上传文件会直接报错。~~ **已修复**：requirements.txt 已声明 `python-multipart>=0.0.9`。
2. ~~**PDF 依赖名错配**：代码用 `PyPDFLoader`，新版依赖的是 `pypdf`；requirements 里写的是已停更的 `PyPDF2`。~~ **已修复**：requirements.txt 改为 `pypdf>=4.0`。
3. ~~**缺 `jq`**：JSONLoader 顶层 import jq，未声明则 .json 上传必失败。~~ **已修复**：requirements.txt 已声明 `jq>=1.7` 并注释 Windows 编译提示。


**B. 正确性与一致性**
5. **幻觉判定脆弱**：`detect_hallucination` 靠子串 `'"is_hallucination": true'` 匹配，LLM 输出带空格（`"true"` 前空格不影响，但 JSON 外裹 ```json 代码块、键名中文、值大写等）会漏判，漏判即放行幻觉回答；达 max_retry 强制放行时也没有任何标记透出给前端。
6. **置信度是 LLM 自评**：与检索质量无关；正则「提取第一个数字」在模型输出含其他数字时可能误取；无密钥恒 0.5。
7. **维度自愈的副作用**：`_check_embedding_compatibility` 检测到维度变化会 drop 整个 collection，但 SQLite 的 uploaded_docs 记录不清理，造成「文档列表有记录、向量库实际为空」，且没有任何自动重建（需手工重新上传）。已部分缓解：文档删除接口的 source 兜底匹配可清掉这类残留记录对应的向量，删记录后重新上传即可恢复。
8. **重复上传不去重**：同一文件多次上传会重复切片入库，检索时重复内容占位（曾导致 top_k 全部被同一文档副本占满、去重后只剩 1 个来源的线上问题，已通过清理重复数据解决，但入库去重逻辑仍未实现）。
9. 死配置/死代码：`RAGConfig.temperature/max_tokens/search_type` 从未生效；`Retriever.retrieve_with_scores` 无调用方；main.py 顶部 `RunnablePassthrough/RunnableLambda` 导入未使用。（注：早期版本的 Groq/llama-3.3-70b 陈旧注释、langchain-groq 依赖、file_loader.py「方案二」注释死代码均已清理。）

**C. 并发与性能**
10. **全局锁串行化所有问答**：`_config_lock` 覆盖整个 `rag.query`（含多次 LLM 远程调用，单次数秒），并发问答排队，吞吐低。
11. **参数覆盖机制脆弱**：基于可变单例 + 手工快照/恢复 5 个字段，新增参数漏快照就跨请求泄漏；chat 历史全量从 SQLite 拉取（实际只用最近 4 轮）。
12. **本地重排模型每次调用重新加载**：走 `_rerank_local` 降级路径时 `CrossEncoder(model_name)` 在函数体内实例化，无缓存，重复占内存/CPU（默认路径已是硅基流动 API、不加载本地模型，此问题仅在 API 不可用的降级场景出现）。
13. Multi-Query 去重是 page_content 精确匹配，差一个标点即视为不同文档；`retrieve_multi` 达 top_k*3 即 break，靠后的子查询可能不执行。

**D. 安全与运维**
14. 无任何鉴权/限流；上传文件名未做路径字符清洗（拼 tmp 路径时理论可构造 `../`）；文件内容只做后缀与大小校验，不做内容校验。
15. Docker 单容器双进程无看护（uvicorn 挂了容器仍活着）、未声明数据 volume、streamlit 未 headless；无 .dockerignore（构建上下文可能带入 chroma_db/venv）。
16. ~~无文档/会话删除接口，数据只增不减~~ **已实现**：`DELETE /api/doc/{doc_id}`（两级匹配清理 Chroma 向量 + SQLite 元数据）与 `DELETE /api/session/{session_id}`（级联删消息）；遗留点：日志文件会记录用户查询内容（隐私）；source 兜底删除需 `store.get()` 拉取全部 metadata 在 Python 端过滤，向量库规模大时有一次性内存/IO 开销（doc_id 精确路径不受影响）。

### 5.2 优化方向

**检索侧**
- 引入**混合检索**：BM25（关键词，匹配专有名词/编号）+ 向量（语义）分数融合，Chroma 不支持 BM25 可加 rank_bm25 或换 Milvus/ES；
- 召回与精排解耦：多路召回放大候选（如 top_k×5）再交给 reranker 精排到 top_k，而不是召回 k 条、重排还取 k 条；
- 上 **MMR**（RAGConfig 已留 search_type 字段）去冗余、**HyDE** 假设文档增强召回；
- 父子分块（小块检索、大块返回上下文）、按文件 metadata 过滤的知识库隔离。

**重排侧**
- 中文场景换 `bge-reranker-v2-m3`，与 bge-m3 Embedding 配套 **已实现**：本地 CrossEncoder 优先，硅基流动 `/rerank` API 兜底；
- CrossEncoder 本地路径为**模块级懒加载单例**（进程内只加载一次），加载失败结果亦缓存，避免反复重试阻塞请求；
- 召回与精排候选数已可解耦配置（API 全量打分、本地截取）。

**生成与反思侧**
- 幻觉/置信度 LLM 调用改用**结构化输出**（Pydantic parser / function calling），替换子串匹配；
- 重试时除 feedback 外可追加「逐句引用来源」要求，或对回答句子做逐条 entailment 校验；
- 向 CRAG 靠拢：加**检索质量评估器**，召回不可信时明确回复「知识库未覆盖」而非硬答；
- 把 temperature/max_tokens 真正传入 init_chat_model；置信度融合「检索距离分数 + LLM 自评」。

**存储与数据治理**
- ~~chunk metadata 写入 doc_id 并与 uploaded_docs 关联，支持按文档删除~~ **已实现**：上传注入 doc_id，删除接口两级匹配（doc_id 精确 + source 兜底）清理 Chroma 与 SQLite；
- 待做：内容哈希去重（重复上传仍会重复入库）；
- 维度切换自愈后自动触发「按 SQLite 记录重新索引」或至少同步标记文档状态；
- ~~会话/消息清理接口~~ **已实现**：`DELETE /api/session/{id}` 级联删除消息；待做：保留周期/批量归档策略。

**工程化**
- 参数隔离：把运行参数作为 `rag.query(question, history, runtime_params)` 的入参，每请求独立 config，**删掉全局可变单例覆盖和全局锁**（或锁只护 Chroma 写入）；LLM 调用改 async，利用 FastAPI 异步并发；
- ~~修正依赖（python-multipart、pypdf、jq，移除 langchain-groq）~~ **已完成**；smoke 脚本改造为 pytest 用例进 CI；
- Docker 拆 compose 双服务或加 supervisord，声明 volume、--server.headless、.dockerignore；
- 上传文件名用 `Path(filename).name` 清洗；加接口鉴权；日志脱敏。
